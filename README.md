# Scalar-Vector-Graphics
This project focuses on generating Scalable Vector Graphics (SVG) using C++ by designing a set of object-oriented classes that parse shape definitions from a text file and output them into a valid HTML SVG file.
